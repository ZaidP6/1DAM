package ejemplo02stream;

public class Principal {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

		// Para usar un Stream siempre se tiene primero la lista 
		// de los que sea y luego "." y se llama al stream: "lista.stream()..."

		// EJ: metodoX{return CrudCoche.buscarVendidos().stream().count();}
		// listaCoche.stream()
		//	.filter(coche->coche.getModelo().equalsIgnoreCase.limit(limit).list()); Esto es continuación de la líne ade arriba
		
		
	}

}
